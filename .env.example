# Salin file ini ke .env lalu isi nilainya:
#   cp .env.example .env

# MySQL / MariaDB (Niagahoster cPanel → MySQL Databases)
# Format: mysql://USER:PASSWORD@HOST:3306/DATABASE
# Di shared hosting Host sering "localhost"
DATABASE_URL="mysql://u6424712_kas:PASSWORD@localhost:3306/u6424712_kas"

# Rahasia sesi/cookie — WAJIB diganti di produksi
# Generate: openssl rand -base64 32
AUTH_SECRET=

NODE_ENV=production

# Opsional — default Next.js: 3000
# PORT=3000
